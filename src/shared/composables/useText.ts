const useText = () => {
  const limitText = (text: string, limit: number): string => {
    const limitedText = ''
    console.log(text, limit)
    return limitedText
  }
  return { limitText }
}

export default useText
